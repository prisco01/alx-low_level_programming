#include <stdio.h>

/**
 *   main - prints the alphabet in lowercase.
 *
 *   followed by a new line
 *
 *   Return: Always 0 
 */

int main(void)

{
	char ch;

	for (ch = a; ch <= z; letter++)
		putchar(letter);

	putchar('\n');

	return (0);

}
