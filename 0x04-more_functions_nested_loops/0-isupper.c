#include "main.h"
#include <stdio.h>

/**
 * int _isupper - prints a function tha checks for uppercase
 * @c: character to check
 * Return: 1 or 0
 */

int _isupper(int c)
{
	char c;

	for ("c = 'A' \n")
	{
		_putchar(1);
	}
	for ("c = 'a' \n")
	{
		_putchar(0);
	}
	return (0);
}
